# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea
/@tmp

# Local env files (real secrets) — keep the *.example templates tracked.
.env
.env.*
!.env.example
!.env.alfabankby.example
!.env.priorbank.example

# Auto-generated by @nuxt/test-utils on test run
.nuxtrc

# Visual-verification output (see docs/VISUAL_VERIFICATION.md) — never commit:
# screenshots of a real portal can leak CRM data, domains, tokens.
screenshots

# Live Alfa demo output (full unmasked statement data) — never commit
alfa-demo-output*.json

# Live Priorbank demo output + generated signing key/jwks — never commit
prior-demo-output*.json
prior_private_key.pem
prior_jwks.json


node_modules
.git
.nuxt
.output
.env
.env.*
!.env.example
dist
*.log
.DS_Store

# Vendored reporting/agent bundle — docs & scripts, not part of the web build
reporting-kit
screenshots
# Эталоны визуальных регресс-тестов — несколько мегабайт PNG, в образе не нужны, а в
# build-context попадали бы на каждой сборке.
tests
telemetry-station
deploy/

<script setup lang="ts">
// Раздел «Подключение банка» (#530).
//
// ⚠ Секции вынесены в отдельные компоненты не ради размера файла: навигация показывает ОДИН
// раздел, а посещённый остаётся смонтированным (`KeepAlive` в `SettingsForm`) — а кэшировать
// `KeepAlive` умеет только компоненты, не голые узлы разметки. Здесь это принципиально: сверка
// счетов ходит в БАНК, и повторный монтаж при каждом переключении вкладки бил бы по его лимитам.
</script>

<template>
  <div class="space-y-4">
    <!-- Онлайн-подключение (Альфа / Приор) + ручной опрос. Оба admin-only и гейтят себя сами;
         вне фрейма портала показывают инертное объяснение. -->
    <BankConnectCard />
    <PollNowButton />
  </div>
</template>

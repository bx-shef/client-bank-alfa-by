/**
 * Тексты состояния «настройки прочитать не удалось» (#705).
 *
 * Живут в чистом модуле, а не в компоненте, по одной причине: их ДВА потребителя — форма
 * настроек (алерт над разделом) и сам композабл (сообщение отказа при попытке сохранить), —
 * а разойдясь, они описали бы разные миры: на экране «не удалось прочитать», в ответе на
 * нажатие — что-нибудь про сеть.
 */

/** Заголовок отказа. Говорит о ЧТЕНИИ, а не о настроенности портала. */
export const SETTINGS_LOAD_FAILED_TITLE = 'Не удалось прочитать настройки'

/**
 * Почему сохранение запрещено. Формулировка называет ПОСЛЕДСТВИЕ, а не запрет: «сохранение
 * недоступно» человек читает как поломку кнопки и жмёт ещё раз, а «перезапишет настройки
 * портала пустыми» объясняет, почему ждать — правильное действие.
 */
export const SAVE_BLOCKED_MESSAGE
  = 'Настройки не прочитаны, на экране значения по умолчанию — сохранение перезаписало бы ими настройки портала. Обновите страницу и попробуйте снова.'

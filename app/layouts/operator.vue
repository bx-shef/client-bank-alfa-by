<script setup lang="ts">
// Layout служебных экранов ОПЕРАТОРА (`/login`, `/queues`) — обычная вкладка, не портал.
//
// ⚠ Разметка сегодня совпадает с портальным layout'ом (обе оболочки — `AppShell`), и это не повод
// оставлять одно имя на двоих: страницы отличаются авторитетом (наша сессионная кука против
// фрейм-токена портала), поведением (`AuthGate` против `InPortalGate`) и тем, что здесь нет и не
// будет ни `fitWindow`, ни слайдеров.
//
// ⚠ `AuthGate` не поднят сюда по той же причине, по которой в портальном не поднят `InPortalGate`,
// только более наглядной: `/login` — сам вход, и гейт на нём означал бы страницу, которая требует
// войти, чтобы показать форму входа.
</script>

<template>
  <AppShell>
    <slot />
  </AppShell>
</template>

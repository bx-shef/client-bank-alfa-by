# Замечания по вёрстке приложения

> Last reviewed: 2026-08-16

## Не выключена метрика на страницах приложения

http://localhost:3000/app/?preview=1

в консоли показало проблему https://mc.yandex.ru/metrika/match.html
```
GET https://hdrc.yandex.net/ net::ERR_CERT_AUTHORITY_INVALID
```

## На главной Нет выборки последних операций

На главной странице приложения последние операции не выгружаются.
Не понятно почему `ImportStatusBanner` не завязан на последние операции.
`Последние операции` - стоит использовать компонент - нужно смотреть на ручной импорт

## На главной банер о доработках
- ссылка не правильная
- вынести банер в импорт прайсов

## На главной вернуть виджет обратной связи

`FeedbackWidget` - на главной нужен что бы сказать - вот это вау приложение - или опа не досмотрели вот эту штучку - и дать обратную связь 
Посмотри - в настроках самое место - ооо мой отзыв - у меня не работает вот эта настройка или я хочу вот такую настройку



## На главной пагинация

Как то дергано она работает

## Настройки
- запретить ручную загрузку - разрешить такую операцию
- не показывать банер про последние импорты `Ещё не запускалась` - я так понял что это про импорт по крону - если нет настроект банков.

## Права доступа
- проверить ограничения по отделу - браузер - моб-прложение
- проверить ограничения по 2м юзерам - браузер - моб-прложение
- дописать в описание процесса

## На галвной моб приложение
Помни что определить где нах код работает - ты можешь используя композейбл от uiB24
- кнопку импорта ручного - вынести из меню
- скрыть заголовок - дубль выходиты
- настройки скрыть

## Общие странности приложени
- layout: 'clear' - почему clear- это во всех страницах приложения используется
- стоит пройтись по утилитам и композейблам - старый код стоит убрать

## настроеки
- стоит посмотреть в репо импорта прайсов - у них при настройке чатов все сипотно показывается - у нас это не работает при показе - показывает не красивое название - и хочется еще кнопку сбросить околовсе селектов в настройках посемтить по образу и подобию как в репо импорта прайсов 
- выровнять по центру `загрузку` и `проверку` и `Настройки доступны только администратору` - стоит смотреть на B24DashboardPanel в том числе
- выровнять по центру `загрузку` и `проверку` и `Настройки доступны только администратору` - стоит смотреть на B24DashboardPanel в том числе

- У тебя сказано что на странице настроек - есть 3 варианта открытия
```
Как экран открыт — от этого зависит, что делает закрытие:
 * слайдером (`place = app-options`) → закрываем оверлей портала;
 * обычной навигацией внутри фрейма (портал отказал в слайдере) → возвращаемся на /app;
 * вне портала → никуда не уходим, закрывать нечего.
 *  ⚠ `null` — «ещё не знаем»: со стартовым `false` первый кадр считал бы себя standalone.
```
Переделай - только 2 состояния для настроек - слайдер и вне б24 

Так же этот блок стоит поменять на Toast
```ts
<div class="flex items-center justify-center gap-2">
  <span
    v-if="1 > 0 || savedOk && !saving"
    class="text-sm text-(--ui-color-accent-main-success)"
    role="status"
    aria-live="polite"
    data-testid="save-status"
    >Сохранено ✓</span>
  <span
  v-else-if="error && !saving"
    class="text-sm text-(--ui-color-accent-main-alert)"
  role="status"
  aria-live="polite"
  data-testid="save-status"
  >{{ error }}</span>
</div>
```

## Главная страница (index)
Нужно как и в приложении импорта прайсов - открывалась главная страница и открывала слайдер для страницы app. 

## Логирование
Это у тебя по коду идет.
```ts
console.warn
```
Товарищ - у тебья есть jssdk - в нем крйтой логгер - перепиши все на логер из jsSdk 

Собственно конструкцию типа
```ts
if (import.meta.dev) console.warn('[settings] B24 init failed', e)
```
Ты сможешь настроить логером - что и куда логировать на каком уровне

## компонент готовности настроек
`:description="!(setup.loaded.value) ? '...' : ready ? 'всё настроено' : `осталось: ${pending}`"` - это лучше в computed сделать

## страница настроек
Стоит компонент настроек разделить на несколько страниц
- смотреть в репо дашборда настроки
- на каждой оставить блок готовности
- критерий разделения - это текущий акордион - вот его и преобразовать стоит

Обоснование - что то все сложно - настроки - подключение импорта - чаты и тп - упрощаем

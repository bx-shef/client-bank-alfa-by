# Карта продукта — демо на лендинге

> Last reviewed: 2026-07-09

Рабочий журнал и карта задачи «демо-версия на публичном лендинге». Сверяюсь с ним перед
каждым куском работы и держу синхронно с [`project-map.md`](project-map.md) и [`CLAUDE.md`](../CLAUDE.md).

## Цель (что просил владелец)

1. **Демо разбора файла на лендинге.** Пользователь прикрепляет файл выписки → получает
   ответ: *что в файле* и *какие данные извлечём* (операции, контрагент, суммы, назначение,
   распознанные идентификаторы). Разбор — **в браузере**, детерминированный (без backend/AI).
2. **Рабочий пример «песочницы» к Альфе и Приору** на демо-странице. Живой OAuth банков
   гео-блокируется из облака (только из РБ), поэтому «рабочий» = кнопки прогоняют
   **реальные нормализаторы** (`normalizeAlfa`/`normalizePrior`) на образцах ответов
   sandbox-формата → тот же экран извлечения. Это честно демонстрирует весь путь
   «ответ банка → нормализованные операции», не завися от гео-блока.
3. Дизайн/тексты/уведомления — «потом поправим». Приоритет: **всё должно отработать**.

## Принятые решения

- Демо — **секция на существующем лендинге** (`app/pages/index.vue`, layout `landing`,
  тёмная брендовая тема), а не отдельная страница — одна точка входа, меньше дубля.
- Вся логика извлечения — в чистом `app/utils/demoExtract.ts` (покрыт тестами):
  образцы Альфа/Приор, сводка извлечения, распознавание идентификаторов по матрицам.
- Файловый разбор переиспользует готовое ядро `importUpload.ts` (`processUploadBatch`,
  windows-1251, дедуп) — то же, что на `/import`.
- Распознавание — реальный `recognizeByMatrices` (`purposeMatch.ts`) на демо-наборе матриц.
- Рендер результатов — самодостаточный тёмный UI внутри `LandingDemo.vue` (не тащим
  b24ui-`OperationList`, чтобы не смешивать light/dark-токены с брендовой оболочкой).

## План (чек-лист)

- [x] Изучить существующий разбор/нормализаторы/лендинг
- [x] Создать эту карту продукта
- [x] Чистое ядро `demoExtract.ts` + тесты (10 тестов)
- [x] Компонент `LandingDemo.vue` (файл + песочница Альфа/Приор + панель извлечения)
- [x] Тексты подводки/ошибок в `landing.ts` (`LANDING_DEMO`); интерактивные подписи пока в компоненте (черновик)
- [x] Встроить секцию в `index.vue` (`#demo` + кнопка в hero)
- [x] Nuxt render-тест компонента (6 тестов)
- [x] `pnpm check` (lint + typecheck + test) зелёный — 1016 тестов
- [x] Скриншоты (desktop × dark; лендинг force-dark) — визуальная верификация: пусто/Альфа/Приор/файл
- [x] Прогон 5 ревьюеров, устранение замечаний (таблица ниже)
- [x] Мобильный скриншот (`demo-alfa-mobile.png`)
- [ ] Обновить `project-map.md`/`CLAUDE.md`, коммит, (мерж по разрешению)

## Ревью (5 проверяющих) и что исправлено

Прогнаны 5 параллельных ревьюеров (корректность ядра · Vue-компонент · согласованность/переиспользование ·
безопасность · покрытие тестами). Устранено:

| # | Замечание (важность) | Исправление |
|---|----------------------|-------------|
| 1 | **Гонка (high):** медленный разбор файла затирал результат позже нажатой кнопки банка | Токен `runSeq`: асинхронный разбор отбрасывает свой результат, если его перебила новая команда; кнопки банков `:disabled` во время разбора |
| 2 | Терялся `truncated` (med) — лишние файлы исчезали молча | Показываем «Взяли не все файлы…» (`demo-truncated`) |
| 3 | `<input>` не сбрасывался (med) — повторный выбор того же файла не срабатывал | Сброс `input.value=''` в `onInput` |
| 4 | Дубль `yieldToLoop` (med) со `StatementUpload` | Вынесен `deferToEventLoop` в `importUpload.ts`, переиспользуется в обоих |
| 5 | **Приватность (med):** Яндекс-вебвизор (session-replay на лендинге) писал бы разобранные данные в DOM, а текст обещал «ничего не отправляется» | Текст уточнён: «сам файл не загружается на сервер» (правда — файл не грузится). Маскировка блока результатов в вебвизоре — **follow-up владельца** (ниже) |
| 6 | Рендер-DoS (low) — список операций без ограничения | Кап `MAX_RENDERED_OPS=100` + «…и ещё N операций» |
| 7 | `reachGoal('demo_file')` на ошибке (low) | Перенесён в ветку успеха |
| 8 | Нет SR-фидбека занятости (low) | `:aria-busy` на live-регионе |
| 9 | Флоат-накопление сумм + вводящий в заблуждение комментарий (low) | Округление сумм до 2 знаков в чистом слое (`round2`), комментарий поправлен |
| 10 | Тексты ошибок в компоненте (low) | Вынесены в `LANDING_DEMO` (`parseError`/`noOperations`/`hint`) |
| 11 | Отступ секции расходился с соседними (low) | `mb-12 sm:mb-14` |
| 12 | Пробелы в покрытии (med) | Тесты: пустая выписка, контрагент без реквизитов, округление; reset, ошибка разбора файла, значение распознанного id |

Не менял по решению (обоснованно): свой рендер операций вместо `OperationList` (тёмная брендовая тема,
b24ui-токены не подходят) — осознанно; ключ уникальности контрагента `unp||account||name` — в пределах
одной выписки реквизиты стороны консистентны, first-present стабилен.

### Второй цикл проверки (5 проверяющих) — верификация после правок

Перепроверка исправлений + поиск противоречий/ошибок/безопасности. Вердикты: интеграция чистая
(lint+typecheck), безопасность ок (XSS/эксфильтрация/ReDoS/proto — чисто), тесты надёжны (не флаки),
исправления гонки/округления/сброса `<input>` — корректны. Доисправлено:

| Замечание (важность) | Исправление |
|----------------------|-------------|
| Список «Распознали» рендерился **без капа** (low, self-DoS — как список операций) — флагнули 2 ревьюера | Кап `visibleRecognized`/`hiddenRecognizedCount` (`MAX_RENDERED_OPS`) + кап бейджей в строке (`MAX_RECOGNIZED_IDS_PER_ROW`, «+N») |
| **Противоречие (med):** доки утверждали «тексты — `LANDING_DEMO`», но ~20 подписей захардкожены в компоненте | Формулировки в `CLAUDE.md`/`project-map.md` уточнены: в `LANDING_DEMO` — подводка/ошибки, интерактивные подписи пока в компоненте (черновик) |
| Устаревшие счётчики тестов в карте (low): «7»/«4» | → 10 / 6 (фактические) |
| Пред-существующая ошибка в `CLAUDE.md` (low): «Почему мы (4 карточки)» | → 6 карточек (по `LANDING_FEATURES`) |

## Follow-up (владельцу / позже)

- **Вебвизор Метрики + приватные данные:** на лендинге активна Яндекс.Метрика с webvisor (session-replay).
  Демо рендерит разобранную выписку в DOM. Если пользователь загрузит **реальную** выписку, вебвизор её
  запишет. Мера: добавить блоку результатов (`[role=status]` в `LandingDemo.vue`) класс контент-хайдинга,
  настроенный в счётчике Метрики, либо отключить вебвизор на этой странице. Сам файл при этом не грузится.

## Итог по функциональности (проверено вживую в браузере)

- **Файл → извлечение** ✅ — реальная фикстура `demo-type4-alfa.txt` разобрана в браузере
  (windows-1251), показаны 3 операции, УНП/счета/назначения, суммы, сводка.
- **Песочница Альфа** ✅ — образец `partner.accounts` прогнан через **реальный** `normalizeAlfa`;
  3 операции, распознаны `СЧ-1042` (смарт-счёт) и `ЗК-2050` (заказ).
- **Песочница Приор** ✅ — образец Open Banking через **реальный** `normalizePrior`; 2 операции,
  верная сторона контрагента (debtor/creditor), распознаны `СЧ-2077` и документ (демонстрирует
  сворачивание гомоглифов: латинский `BOPC-114/26` → кириллический `ВОРС-114/26`).

## Журнал

- 2026-07-09 — старт. Изучены `importUpload.ts`, `StatementUpload.vue`, `index.vue`,
  нормализаторы Альфы/Приора, `purposeMatch.ts`. План зафиксирован. Ветка
  `claude/landing-demo-file-upload-ffk1az`.
- 2026-07-09 — реализовано ядро `demoExtract.ts` (образцы Альфа/Приор через реальные
  нормализаторы, сводка извлечения, распознавание по демо-матрицам) + `LandingDemo.vue`
  (тёмный самодостаточный UI) + секция `#demo` на лендинге + тексты. `pnpm check` зелёный
  (1011 тестов). Скриншоты сняты и проверены — все три источника отрабатывают. Дальше — 5 ревьюеров.
